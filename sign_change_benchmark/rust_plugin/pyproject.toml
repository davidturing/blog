[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "sign_change"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "maturin>=1.12.6",
    "polars>=1.0.0",
]

# World Grounding & Proactive Exploration System Dependencies

# Core dependencies
tomli>=2.0.1
numpy>=1.21.0
requests>=2.28.0
feedparser>=6.0.0

# Embedding and NLP
sentence-transformers>=2.2.0
transformers>=4.30.0

# ANE acceleration (optional, for Apple Silicon)
coremltools>=7.0  # Only needed if using ANE

# Docker for sandboxing
docker>=6.0.0

# Logging and utilities
python-dateutil>=2.8.0

# Testing dependencies
pytest>=7.0.0
pytest-asyncio>=0.20.0

# Note: Some dependencies are optional based on system capabilities:
# - coremltools: Only required on Apple Silicon Macs with ANE support
# - docker: Required for sandbox validation (system dependency)

# Installation notes:
# 1. On Mac mini M4, ensure you have Docker Desktop installed
# 2. For ANE acceleration, make sure you're running on macOS with Apple Silicon
# 3. Virtual environment recommended: python3 -m venv venv && source venv/bin/activate
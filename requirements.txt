# World Grounding & Proactive Exploration System Dependencies
# Optimized for Mac mini M4 environment

# Core dependencies
tomli>=2.0.1            # TOML parsing for configuration
lancedb>=0.5.0          # Disk-based vector database for low memory usage
sentence-transformers>=2.2.0  # For embeddings (supports ANE acceleration)
torch>=2.0.0            # PyTorch with ANE support on macOS
feedparser>=6.0.0       # RSS/Atom feed parsing
aiohttp>=3.8.0          # Async HTTP client for efficient network requests
docker>=6.0.0           # Docker SDK for shadow sandbox isolation
pydantic>=2.0.0         # Data validation and settings management

# Optional dependencies (for specific sources)
PyGithub>=1.55          # GitHub API client
praw>=7.0.0             # Reddit API client
beautifulsoup4>=4.10.0  # HTML parsing for doc crawling

# Development and testing
pytest>=7.0.0
black>=22.0.0           # Code formatting
mypy>=0.950             # Type checking

# Note: All dependencies are chosen to be lightweight and compatible with
# macOS ARM64 (M-series chips) and ANE acceleration where possible.